// Global `IconGlyph`: mapeia o slug de `Category.icon_name` para o
// arquivo SVG correspondente em `desktop/ui/assets/icons/categories/`.
//
// As SVGs aqui ESPELHAM exatamente as paths em
// `web/src/components/category_icon.rs`, garantindo que desktop e web
// renderizem o mesmo desenho para o mesmo slug.
//
// Slug desconhecido cai em `placeholder.svg` (círculo neutro).
// O elemento `Image` consumidor pode aplicar `colorize` para tingir o
// SVG (todos os ícones usam a mesma cor base `#1f2937`, então a
// substituição por cor única é uniforme).
export global IconGlyph {
    pure public function image-for(slug: string) -> image {
        return slug == "ice-cream"   ? @image-url("assets/icons/categories/ice-cream.svg")
             : slug == "drink"       ? @image-url("assets/icons/categories/drink.svg")
             : slug == "pizza"       ? @image-url("assets/icons/categories/pizza.svg")
             : slug == "burger"      ? @image-url("assets/icons/categories/burger.svg")
             : slug == "combo"       ? @image-url("assets/icons/categories/combo.svg")
             : slug == "snack"       ? @image-url("assets/icons/categories/snack.svg")
             : slug == "dessert"     ? @image-url("assets/icons/categories/dessert.svg")
             : slug == "candy"       ? @image-url("assets/icons/categories/candy.svg")
             : slug == "coffee"      ? @image-url("assets/icons/categories/coffee.svg")
             : slug == "bread"       ? @image-url("assets/icons/categories/bread.svg")
             : slug == "salad"       ? @image-url("assets/icons/categories/salad.svg")
             : slug == "meat"        ? @image-url("assets/icons/categories/meat.svg")
             : slug == "convenience" ? @image-url("assets/icons/categories/convenience.svg")
             : @image-url("assets/icons/categories/placeholder.svg");
    }
}
